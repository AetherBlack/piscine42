#include <unistd.h>

void *ft_print_memory(void *addr, unsigned int size)
{
    char dotdot[] = ":";
    unsigned int i = 0;

    if (size == 0x0) return addr;

    while (i != size)
    {

    }

    return addr;
}

int main(void)
{
    return 0;
}